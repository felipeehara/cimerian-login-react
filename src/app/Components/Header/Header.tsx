import { Navbar } from "../Menu/NavBar";
import { Profile } from "../Profile/Profile";

export const Header = () => {
    return(
        <div>
            <Navbar/>
            
        </div>
    );
}